@using LiquorStoreFinalProject.Areas.Admin.ViewModels.Product;
@model CreateProductVM
@{
    ViewData["Title"] = "Create";
}



<div class="container my-5">
    <h1>Create Product</h1>
    <form asp-controller="Product" asp-action="CreateProduct" method="post" enctype="multipart/form-data">
        <div class="form-group mt-3">
            <label for="Name">Name</label>
            <input asp-for="Name" class="form-control" name="Name" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="form-group mt-3">
            <label for="Description">Description</label>
            <textarea asp-for="Description" class="form-control" name="Description"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>
        <div class="form-group mt-3">
            <label for="Price">Price</label>
            <input asp-for="Price" type="number" step="any" class="form-control" name="Price" />
            <span asp-validation-for="Price" class="text-danger"></span>
        </div>

        <div class="form-group mt-3">
            <label for="CategoryId">Category</label>
            <select asp-for="CategoryId" class="form-control" name="CategoryId">
                @if (ViewBag.Categories != null)
                {
                    foreach (var category in ViewBag.Categories)
                    {
                        <option value="@category.Id">@category.Name</option>
                    }
                }
            </select>
            <span asp-validation-for="CategoryId" class="text-danger"></span>
        </div>

        <div class="form-group mt-3">
            <label for="DiscountId">Discount</label>
            <select asp-for="DiscountId" class="form-control" name="DiscountId">
                @if (ViewBag.Discounts != null)
                {
                    foreach (var discount in ViewBag.Discounts)
                    {
                        <option value="@discount.Id">@discount.Name</option>
                    }
                }
            </select>
            <span asp-validation-for="DiscountId" class="text-danger"></span>
        </div>


       @* <div class="form-group mt-3">
            <label for="DiscountId">Discount</label>
            <select asp-for="DiscountId" class="form-control" asp-items="ViewBag.Discounts" name="DiscountId"></select>
            <span asp-validation-for="DiscountId" class="text-danger"></span>
        </div>*@
        <div class="form-group mt-3">
            <label for="Image">Image</label>
            <input asp-for="Image" type="file" class="form-control" name="Image" />
            <span asp-validation-for="Image" class="text-danger"></span>
        </div>
        <div class="form-group mt-3">
            <button type="submit" class="btn btn-success">Create</button>
        </div>
    </form>

</div>

@*@section AdminScripts{
    <partial name="_ValidationScriptsPartial" />
}*@

